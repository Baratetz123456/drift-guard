name: Deploy — dev

on:
  push:
    branches: ["main"]
  workflow_dispatch: # Allow manual dev deploys

concurrency:
  group: deploy-dev
  cancel-in-progress: false # Never cancel an in-flight deploy

env:
  AWS_REGION: ap-southeast-1
  ENVIRONMENT: dev
  TABLE_NAME: DriftGuard-dev
  STATE_MACHINE_NAME: driftguard-collection-dev
  FRONTEND_BUCKET: driftguard-frontend-${{ secrets.AWS_ACCOUNT_ID }}-dev
  SNAPSHOT_BUCKET: driftguard-snapshots-${{ secrets.AWS_ACCOUNT_ID }}-dev
  CLOUDFRONT_DISTRIBUTION_ID: ${{ secrets.CLOUDFRONT_DISTRIBUTION_ID_DEV }}

jobs:
  # ─────────────────────────────────────────────
  # Guard: only deploy if CI passed
  # ─────────────────────────────────────────────
  ci-gate:
    name: CI gate (must pass before deploy)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: "backend/requirements/dev.txt"

      - name: Install dev deps
        working-directory: backend
        run: pip install -r requirements/dev.txt

      - name: Lint — ruff
        working-directory: backend
        run: ruff check .

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: "frontend/package-lock.json"

      - name: Install frontend deps
        working-directory: frontend
        run: npm ci

      - name: Type check — tsc
        working-directory: frontend
        run: npx tsc --noEmit

  # ─────────────────────────────────────────────
  # Backend: Package + Deploy Lambda functions
  # ─────────────────────────────────────────────
  deploy-backend:
    name: Deploy backend — Lambda functions
    runs-on: ubuntu-latest
    needs: ci-gate
    environment: dev
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: "backend/requirements/base.txt"

      - name: Install base dependencies into vendor/
        working-directory: backend
        run: pip install -r requirements/base.txt -t vendor/

      - name: Build backend deployment zip
        working-directory: backend
        run: |
          mkdir -p staging_deploy
          cp -r functions staging_deploy/
          cp -r shared staging_deploy/
          cp -r vendor/. staging_deploy/
          cd staging_deploy && zip -r ../../driftguard-backend.zip . -x "*.pyc" -x "*/__pycache__/*"
          cd ../..

      - name: Upload deployment zip to S3
        run: |
          aws s3 cp driftguard-backend.zip \
            s3://${{ env.SNAPSHOT_BUCKET }}/deployments/driftguard-backend.zip

      - name: Update Lambda function code — all 10 functions
        run: |
          FUNCTIONS=(
            "driftguard-settings-dev"
            "driftguard-devices-dev"
            "driftguard-commands-dev"
            "driftguard-collection-orchestrator-dev"
            "driftguard-collection-worker-dev"
            "driftguard-snapshots-dev"
            "driftguard-compare-dev"
            "driftguard-ai-analyze-dev"
            "driftguard-history-dev"
            "driftguard-audit-dev"
          )
          for fn in "${FUNCTIONS[@]}"; do
            echo "Updating $fn..."
            aws lambda update-function-code \
              --function-name "$fn" \
              --s3-bucket "${{ env.SNAPSHOT_BUCKET }}" \
              --s3-key "deployments/driftguard-backend.zip" \
              --no-cli-pager
            # Wait for update to complete before moving to next function
            aws lambda wait function-updated --function-name "$fn"
            echo "$fn updated."
          done

      - name: Update Step Functions state machine definition
        run: |
          DEFINITION=$(cat backend/state_machines/collection_workflow.asl.json \
            | sed "s|\${CollectionWorkerArn}|arn:aws:lambda:${{ env.AWS_REGION }}:${{ secrets.AWS_ACCOUNT_ID }}:function:driftguard-collection-worker-dev|g" \
            | sed "s|\${DeltaNetTableName}|${{ env.TABLE_NAME }}|g")
          STATE_MACHINE_ARN="arn:aws:states:${{ env.AWS_REGION }}:${{ secrets.AWS_ACCOUNT_ID }}:stateMachine:${{ env.STATE_MACHINE_NAME }}"
          aws stepfunctions update-state-machine \
            --state-machine-arn "$STATE_MACHINE_ARN" \
            --definition "$DEFINITION" \
            --no-cli-pager

  # ─────────────────────────────────────────────
  # Netmiko Layer: Rebuild only when worker deps change
  # ─────────────────────────────────────────────
  deploy-netmiko-layer:
    name: Deploy Netmiko layer (arm64)
    runs-on: ubuntu-latest
    needs: ci-gate
    environment: dev
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check if worker requirements changed
        id: layer-check
        uses: tj-actions/changed-files@v44
        with:
          files: "backend/requirements/worker.txt"

      - name: Configure AWS credentials
        if: steps.layer-check.outputs.any_changed == 'true' || github.event_name == 'workflow_dispatch'
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Set up QEMU for arm64 cross-compilation
        if: steps.layer-check.outputs.any_changed == 'true' || github.event_name == 'workflow_dispatch'
        uses: docker/setup-qemu-action@v3
        with:
          platforms: arm64

      - name: Set up Docker Buildx
        if: steps.layer-check.outputs.any_changed == 'true' || github.event_name == 'workflow_dispatch'
        uses: docker/setup-buildx-action@v3

      - name: Build Netmiko layer for arm64
        if: steps.layer-check.outputs.any_changed == 'true' || github.event_name == 'workflow_dispatch'
        run: |
          docker run --rm --platform linux/arm64 \
            -v "${{ github.workspace }}/layer-output:/output" \
            public.ecr.aws/sam/build-python3.12:latest-arm64 \
            /bin/bash -c "pip install netmiko -t /output/python/ && echo Build complete"

          cd layer-output
          zip -r ../driftguard-netmiko-layer.zip python/

      - name: Publish new layer version
        if: steps.layer-check.outputs.any_changed == 'true' || github.event_name == 'workflow_dispatch'
        id: publish-layer
        run: |
          LAYER_ARN=$(aws lambda publish-layer-version \
            --layer-name driftguard-netmiko \
            --description "Netmiko SSH layer for DriftGuard (arm64, Python 3.12)" \
            --zip-file fileb://driftguard-netmiko-layer.zip \
            --compatible-runtimes python3.12 \
            --compatible-architectures arm64 \
            --query 'LayerVersionArn' \
            --output text \
            --no-cli-pager)
          echo "layer_arn=$LAYER_ARN" >> $GITHUB_OUTPUT

      - name: Update collection-worker and devices Lambda layers
        if: steps.layer-check.outputs.any_changed == 'true' || github.event_name == 'workflow_dispatch'
        run: |
          SHARED_LAYER_ARN=$(aws lambda list-layer-versions \
            --layer-name driftguard-shared \
            --query 'LayerVersions[0].LayerVersionArn' \
            --output text)

          for fn in driftguard-collection-worker-dev driftguard-devices-dev; do
            aws lambda update-function-configuration \
              --function-name "$fn" \
              --layers "$SHARED_LAYER_ARN" "${{ steps.publish-layer.outputs.layer_arn }}" \
              --no-cli-pager
            aws lambda wait function-updated --function-name "$fn"
            echo "$fn layer updated."
          done

      - name: Skip — worker.txt unchanged
        if: steps.layer-check.outputs.any_changed != 'true' && github.event_name != 'workflow_dispatch'
        run: echo "backend/requirements/worker.txt unchanged — Netmiko layer rebuild skipped."

  # ─────────────────────────────────────────────
  # Frontend: Build + Deploy to S3 + CloudFront
  # ─────────────────────────────────────────────
  deploy-frontend:
    name: Deploy frontend — S3 + CloudFront
    runs-on: ubuntu-latest
    needs: ci-gate
    environment: dev
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: "frontend/package-lock.json"

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      - name: Inject environment secrets into .env.production
        working-directory: frontend
        run: |
          cat > .env.production << EOF
          VITE_API_BASE_URL=${{ secrets.VITE_API_BASE_URL }}
          VITE_COGNITO_USER_POOL_ID=${{ secrets.VITE_COGNITO_USER_POOL_ID }}
          VITE_COGNITO_CLIENT_ID=${{ secrets.VITE_COGNITO_CLIENT_ID }}
          VITE_COGNITO_REGION=${{ env.AWS_REGION }}
          EOF

      - name: Build production bundle
        working-directory: frontend
        run: npm run build

      - name: Sync build to S3 (with cache headers)
        run: |
          # Long-lived cache for hashed assets (JS/CSS chunks)
          aws s3 sync frontend/dist/ s3://${{ env.FRONTEND_BUCKET }}/ \
            --exclude "index.html" \
            --cache-control "public, max-age=31536000, immutable" \
            --delete

          # Short-lived cache for index.html (must always be fresh for SPA routing)
          aws s3 cp frontend/dist/index.html s3://${{ env.FRONTEND_BUCKET }}/index.html \
            --cache-control "no-cache, no-store, must-revalidate"

      - name: Invalidate CloudFront cache
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ env.CLOUDFRONT_DISTRIBUTION_ID }} \
            --paths "/*" \
            --no-cli-pager
